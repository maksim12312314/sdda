import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
    },
    item_name: {
        color: "#FFF",
        fontSize: 20,
    },
    item_price: {
        color: "#FFF",
        fontSize: 20,
    },
});

export default styles;