import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    view: {
    },
    container: {
        width: 140,
        height: 120,
        borderBottomWidth: 1,
        borderColor: "#FFF",
    }
});

export default styles;