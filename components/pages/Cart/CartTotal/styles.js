import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignSelf: "flex-end",
        width: 90,
        borderTopWidth: 1,
        borderColor: "#FFF",
        marginTop: 8,
        alignItems: "center",
    },
    text: {
        color: "#FFF",
    }
});

export default styles;