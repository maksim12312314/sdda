import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignSelf: "flex-end",
        width: 100,
        borderTopWidth: 1,
        borderColor: "#FFF",
        marginTop: 8,
       
    },
    text: {
        color: "#FFF",
    }
});

export default styles;